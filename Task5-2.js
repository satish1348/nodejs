const squareFunction = require('./Task5-1'); // Import the square function from the module

const number = 5;
const result = squareFunction(number);

console.log(`The square of ${number} is: ${result}`);